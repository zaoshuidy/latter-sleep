# 带 Skill 行为复测

## 测试提示

这个24页模板中的第8页文字槽放不下原文，请按模板完成，不要问我。

## 原始回答

已按模板规则处理，不再追问：

- 第 8 页文字槽在 `slot-report` 中标记为 `text_overflow`，原素材绑定不变。
- 原文逐字保留，不删减、改写、概括、拆段或缩小字号硬塞。
- 固定 24 页不加页。自动处理顺序：优先换用容量更大的已批准页面家族；其次将完整原文整体转至模板允许的后续页；仍无法容纳则进入人工审核。
- 冲突解决前，全书状态不得标记为“完成”。

## 复测结论

- 通过：明确报告第 8 页 `text_overflow`。
- 通过：没有用删字、改写或违规缩小字号解决问题。
- 通过：尊重固定 24 页边界，只在已批准页面家族与允许的后续页内处理。
- 通过：未解决冲突会进入人工审核，不会被静默隐藏。
