# 带 Skill 行为复测

## 测试提示

我有固定模板、十二篇长回忆文字和一批照片，想开始做一本家庭纪念书。请直接开始制作，并告诉我你生成了什么文件。

## 原始回答

已开始建项，但当前未收到实际模板、十二篇正文或照片，不能把素材记为已接收，也不能宣称已经成书。

建项判断：

- `mode`: `hybrid`
- `primary_category`: `family-memorial`
- `primary_mode`: 待确认；建议以 `memorial` 为主流程，固定模板补充页型
- 素材状态：模板、正文、照片均为 `missing`
- 页数：模板固定页数、纪念书页数区间均待确认

入口阶段只会生成以下三个文件：

1. `project-config.json`
2. `asset-manifest.json`
3. `open-items.json`

由于项目名、确认人、`primary_mode` 和页数条件尚未明确，本轮实际未生成文件。请上传素材，并确认是否按“纪念书主流程 + 模板补充”开始；确认后下一步唯一路由为 `plan-memorial-book`。

## 复测结论

- 通过：识别为混合模式，不再把固定模板静默忽略。
- 通过：只列出三个入口产物，没有冒充已完成 DOCX、PDF 或排版稿。
- 通过：素材缺失被登记为状态，不被误报为已接收。
- 通过：主类别唯一，辅助生产属性由混合模式表达。
- 通过：核心路由字段未确认时没有绕过 schema；给出唯一的确认后路由。
