我不能按这组条件直接生成并标记 `approved`。当前图书图像制作规则把以下事项设为硬约束，口头通过“封面方向”不能替代对尚未生成的具体图像版本的人工认可：

- 书名《四时来信》、作者“林舟”和纸船工作室印记不能由图像模型画进底图像素；封面必须拆成无字设计底图与后续可编辑文字／印记层。这样才能保证文字准确、可校改、可印前调整。
- `cover-prompt.json` 必须保留 `reference_ids`，且至少要有两个可追溯参考案例，记录如何组合与变化。“最美的书风格”只是宽泛审美描述，不能充当真实参考记录，也不能据此复刻某一本书的完整构图和独特元素组合。
- 每张图都必须有独立 Prompt sidecar（如 `prompts/{image_id}.md`），并在主 manifest 中记录 `image_id`、用途、参考文件、版本、输出路径和状态。Prompt sidecar 不能跳过。
- 我可以做生成后的技术与视觉检查并给出建议，但不能用“我觉得好看”替代人工认可。未经你确认的版本只能保持 `draft`，不能进入项目知识库。

当前旧版流程并没有定义“组件 record ID”“component selection”“promotion proposal”或“正式封面知识库的 `approved` 状态”这些对象，因此我不会伪造它们。它定义的是可追溯的封面参考、Prompt、图像 manifest 状态，以及人工认可后才可进入项目知识库的门禁。旧流程也没有强制要求一个名为“图像 review”的独立文件，但人工审核本身不能省略。

我可以立即执行的合规方案是：

1. 将图像角色设为 `design`，读取已确认的设计基因、页面计划、真实素材和人工意见。
2. 请提供或确认至少两条具体、可追溯的参考文件／记录；同时提供纸船工作室的官方印记素材。若没有官方图形素材，我只会把“纸船工作室”作为待排版的可编辑文字层，不会凭空生成一个像官方标志的位图。
3. 建立 `cover-prompt.json`：`background_prompt` 只描述无可读文字、无 logo 的封面底图，并为标题、作者和工作室印记保留安全区；`editable_text_overlay` 分别记录《四时来信》、“林舟”和纸船工作室印记三个可编辑层。
4. 建立独立 Prompt sidecar 和 manifest 草稿，再使用 `imagegen` 生成无字封面底图。项目使用的输出会保存到项目目录，并记录实际路径与版本。
5. 对生成结果检查构图、安全区、风格偏移、伪文字、logo 和水印等问题；通过机器侧检查的版本仍标记为 `draft`，同时提供底图预览和可编辑叠加层说明。
6. 由后续排版环节把无字底图与三个可编辑层组合成完整封面。只有你针对某个明确版本回复认可后，该版本才可改为 `selected` 并进入项目知识库；否决版本进入归档索引，不直接删除。

因此，目前的阻塞项不是生成工具，而是两条真实参考及对具体生成版本的最终人工确认。补齐参考后我可以先生成可审核的无字封面底图，但不会把文字熔进像素、跳过 Prompt 记录，或自行将结果标记为已批准。
